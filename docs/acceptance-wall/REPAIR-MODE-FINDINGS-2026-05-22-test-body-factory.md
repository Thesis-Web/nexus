# Acceptance Wall — Test-Body Factory Findings 2026-05-22

## F-15 CLOSED (2026-05-22 PRODUCT REPAIR MODE)

**Status:** closed by commit fixing target_systems intersection scoping
in `packages/core/src/identity/delegation-mint.ts` +
`scripts/nexus-main.ts` per owner ruling 2026-05-22.

**Surface fixed:** `BakedDelegationMint.mint()` now reads
`input.requiresSystemAction`. When `true` (NXS / system-action
dispatch) the empty-target_systems check is the unchanged hard
fail-closed. When `false` (model-bound: `kind:nvg`, free_chat,
synthesize-only, `secure_handoff`) the intersection is still computed
verbatim — empty is allowed; the delegation envelope carries the
literal (possibly empty) `allowedSystems`. Downstream NXS gates still
fail-closed on `allowedSystems=[]` if a system action is attempted.
The other five dimensions (capabilities, oct_level, firewall_rights,
run_types, risk_tier) remain symmetric for ALL tasks.

**Wiring:** `makeIssueDelegation` in `scripts/nexus-main.ts` derives
`requiresSystemAction` from `scope.requiresNxs === true` (set by
`buildNodeFromSubTask` when SubTaskDecl.kind === 'nxs').

**Wall delta:**
- Before: 138 tests / 38 pass / 100 fail (HEAD a3ba07e)
- After:  138 tests / 47 pass / 91 fail (HEAD with F-15 fix)
- E2E-02..10 (9 chat-on-prem tests): RED → GREEN
- All `closeReason='error'` (intersection denial) failures eliminated:
  prior pass had 9; this pass has 0
- Remaining chat-style multi-leg tests (E2E-11..20, E2E-31..36/38..39,
  E2E-51..60, E2E-71..80) now fail downstream at `closeReason=null` —
  the next real surface is the kind:nvg dispatch / model-response /
  final_response path, NOT delegation/intersection.

**Tests added (delegation-mint.test.ts):**
- NXS path (requiresSystemAction=true) still fails empty target_systems
- NVG path (requiresSystemAction=false) succeeds with empty target_systems
- NVG path does NOT widen target_systems to all systems or wildcard
- NVG path still fails on empty CAPABILITIES (symmetric on other dims)
- NVG path still fails on empty FIREWALL_RIGHTS (symmetric on other dims)
- NVG path STILL preserves system narrowing when intersection is non-empty
  (no widening even for model-bound tasks)

**Invariants preserved:**
- GOV-04 still passes (six-dim three-way intersection AST scan)
- No wildcard authority added
- No empty-set-as-all-systems widening
- No fail-open path
- NXS gates remain symmetric

---


Generated by Test-Body Factory mode session 2026-05-22. Each finding
documents a product defect surfaced by a newly-bodied acceptance-wall
test. The bodies live at HEAD; the failures are honest production red,
not test stubs.

Convention:
  - `Class:` PRODUCT_RUNTIME | UNIMPLEMENTED_SURFACE | EXTERNAL_DEPENDENCY | HARNESS_GAP | CATALOG_DRIFT | PROCESS_BUG
  - `Severity:` low | medium | high | critical
  - `Status:` OPEN unless the finding has already been logged elsewhere

---

## TB-01 — Output-contract template library not built

- Class: UNIMPLEMENTED_SURFACE
- Severity: high
- Tests exposing it: E2E-41..50 (05-multi-with-contract), E2E-65, E2E-70 (07-branching board_doc_v1), E2E-76 (08-batch batch_summary_v1), E2E-90 (09-multi reconciliation_v1), E2E-56 (06-mixed board_doc_v1)
- First failing surface: `compile_assembly_complete.templateId === 'pass_through'` and `passThrough: true` on every multi-leg run
- Expected production behavior: when a run carries a named `outputContractTemplateId`, compile should hydrate the template, apply guards, and emit `templateId === <expected>` with `passThrough: false`
- Repo evidence read:
  - `packages/contracts/src/externals/output-contract.ts` exposes `outputContractId: Uuid` but the workspace POST schema (`packages/interfaces/api/src/routes/workspace.ts:284-321`) does NOT accept an `outputContractTemplateId` field on `free_text` requests
  - No template registry under `config/output-contracts/` (directory does not exist)
- Status: OPEN

## TB-02 — Workspace POST schema rejects `outputContractTemplateId`

- Class: UNIMPLEMENTED_SURFACE
- Severity: high
- Tests exposing it: same set as TB-01
- First failing surface: even if the template library existed, the strict-mode `FreeTextSchema` does not accept a contract-template field so the route 400s before the planner sees it
- Repo evidence read: `packages/interfaces/api/src/routes/workspace.ts:284-320`
- Expected behavior: a per-run `outputContractTemplateId: string` accepted by all three promptMode schemas, threaded into `WorkspaceRunRequest` for the planner
- Status: OPEN

## TB-03 — Frontier endpoint health/route not exercised by default by NVG classifier

- Class: PRODUCT_RUNTIME
- Severity: medium
- Tests exposing it: E2E-11..20 (02-chat-frontier), E2E-51..58 (06-mixed-tier)
- First failing surface: setting `preferredEndpointId: 'openai-gpt'` either routes to openai successfully (test passes) or NVG demotes to ollama and the test envelope still passes
- Expected behavior: a frontier-tier preferenced run should record `nvg_route` events naming the openai endpoint when the persona's `maxRiskTier` permits; demotion should emit `endpoint_demoted` or `tier_ceiling_exceeded` so the audit trail is explicit
- Repo evidence read: `config/nvg/endpoints.v1.yaml` (openai-gpt enabled, frontier_general tier); orch wrapper at `scripts/nexus-main.ts:1064-1086`
- Status: OPEN (low priority — observation, not a regression)

## TB-04 — Multi-item compile pass-through (F4.12) — CORRECTED 2026-05-22

- Class: (was UNIMPLEMENTED_SURFACE) → **PARTIAL EVIDENCE OF GREEN**
- Severity: was high → re-triaged
- Tests exposing it: previously claimed E2E-40 + the E2E-8x merge tests
- **Correction:** E2E-40 actually PASSES with `itemCount === 2` against the live runtime (verified against `runs/acceptance-wall-2026-05-21/FAILURE-LEDGER.jsonl` at HEAD 4fb0dc8 — E2E-40 does not appear in the failure list). So the strict-asserted single-file two-NXS-leg bundle shape DOES assemble both items. The previous claim that E2E-40 exposed F4.12 was wrong.
- The remaining F4.12 surface area to verify is the merge-with-LLM-node and contract-driven paths (the multi-source merge E2E-81..89 and batch-summary E2E-71..75 still fail, but on `closeReason='error'` upstream of compile — not on `itemCount`). Until those upstream failures are resolved (F-15 cascade), this finding cannot be re-tested against compile's behavior.
- Status: PROVEN for the simple 2-NXS-leg case (E2E-40). UNKNOWN for nvg-mixed compile paths until F-15 unblocks them.

## TB-05 — Gmail / mail connector absent on the seeded connector registry

- Class: CONNECTOR_MISSING
- Severity: medium
- Tests exposing it: E2E-91..100 (10-gmail), E2E-109 (vp external action)
- First failing surface: NXS dispatch against `target.system === 'gmail'` denies at delegation mint (empty intersection on the connector registry side) or at Gate 04 (connector not found)
- Expected behavior: either Mailpit (local, deterministic) or Gmail OAuth registered as a connector with `compose:email` + `read:email` capabilities
- Status: OPEN — Mailpit-first per owner ruling 2026-05-21

## TB-06 — Planner-decomposition checkback emitter absent (HL#4)

- Class: PRODUCT_RUNTIME
- Severity: medium
- Tests exposing it: E2E-66 (ambiguous next-agent), E2E-79 (oversize batch checkback), E2E-112 (HL#4 surface)
- First failing surface: when the planner cannot resolve a next-agent step, the run silently rejects or kills with no `plan_checkback_required` emit; only model-tier health/ceiling paths emit a checkback today (scripts/nexus-main.ts:2037/2102/2152)
- Expected behavior: HL#4 requires the planner to surface a callback for any decomposition-stage uncertainty — ambiguous-next-agent, oversize batch, missing template, etc.
- Status: OPEN — arch patch + emit-path expansion

## TB-07 — Admin revoke endpoint not implemented (HL#14 surface)

- Class: UNIMPLEMENTED_SURFACE
- Severity: medium
- Tests exposing it: E2E-110, E2E-118
- First failing surface: `POST /workspace/admin/principals/<id>/revoke` 404s; the test cannot trigger mid-run RBAC drift
- Expected behavior: admin route to revoke a capability mid-run; runtime emits `claim_drift_detected` when the next gate evaluation sees stale claims
- Status: OPEN — owner ratification needed on (A) test-only harness route vs (B) signed production endpoint vs (C) defer

## TB-08 — Chain-depth ceiling not configured per persona

- Class: UNIMPLEMENTED_SURFACE
- Severity: low
- Tests exposing it: E2E-107
- First failing surface: a 6-node sequential nvg chain accepts without `chain_depth_exceeded` denial — no `maxChainDepth` policy is seeded
- Expected behavior: persona-level maxChainDepth that surfaces a deny event when the requested DAG depth exceeds the ceiling
- Status: OPEN — config + policy seed

## TB-09 — Connector environment tag absent

- Class: UNIMPLEMENTED_SURFACE
- Severity: low
- Tests exposing it: E2E-108
- First failing surface: `environment: 'prod'` in `rawPayload` is ignored by the connector dispatcher (no env-aware ACL)
- Expected behavior: connectors carry an `environment` tag; persona-level firewall rights gate per env
- Status: OPEN — config seed + connector loader change

## TB-10 — SigningCouncil 2-of-2 quorum not attached to any business action

- Class: UNIMPLEMENTED_SURFACE
- Severity: low
- Tests exposing it: E2E-103
- First failing surface: no policy declares `quorum_required: 2_of_2_council` for any business agent action; manager and ceo both fail at capability denial instead of the quorum ceiling
- Expected behavior: a seeded policy attaching SigningCouncil 2-of-2 to a high-risk business action so the test can distinguish capability denial from quorum denial
- Status: OPEN

## TB-11 — checkbackSourceRunId may not record HL#12 chain provenance event

- Class: UNIMPLEMENTED_SURFACE
- Severity: low
- Tests exposing it: E2E-69
- First failing surface: the harness sends `checkbackSourceRunId` on Run B; the schema accepts it (`packages/interfaces/api/src/routes/workspace.ts:317`) but no `checkback_chain_recorded` event has been observed on the ledger
- Expected behavior: when the route receives a non-null `checkbackSourceRunId`, the planner emits an HL#12 provenance event so audit can trace second-run chains
- Status: OPEN

## TB-12 — Mid-DAG conditional edges may not gate downstream nodes

- Class: UNIMPLEMENTED_SURFACE
- Severity: medium
- Tests exposing it: E2E-68 (judge picks downstream agent), E2E-66 (ambiguous next-agent)
- First failing surface: `subTaskEdges` with `edgeType: 'conditional'` and `conditionSpec` accepted by the route but the orch coordinator may run both branches regardless of `conditionSpec` evaluation
- Expected behavior: only the matching branch dispatches; the other is skipped with `branch_skipped_condition_unmatched`
- Status: OPEN — verify orch coordinator branch-evaluator path

---

The wall run that produced this set is captured in
`docs/acceptance-wall/FAILURE-LEDGER-2026-05-22-test-body-factory.{md,jsonl}`
(generated by the AcceptanceWallReporter; see
`tests/e2e/_acceptance/reporter.ts`).
